modules:
  https:
    prober: http
    timeout: 5s
    http:
      method: GET
      fail_if_not_ssl: true
      follow_redirects: false
      preferred_ip_protocol: ip4
      tls_config:
        insecure_skip_verify: false
