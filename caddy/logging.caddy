# DEC-08: también filtrar errores de runtime, no solo omitir access logs.
log default {
	output stdout
	format filter {
		wrap json
		fields {
			request delete
			msg delete
			error delete
			resp_headers delete
		}
	}
}
